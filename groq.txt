from groq import Groq


client = Groq(api_key=os.getenv("GROQ_API_KEY"))

prompt = """
The system prompt here
"""

chat_completion = client.chat.completions.create(
    # Required parameters
    messages=[
        # System message to set assistant behavior
        {
            "role": "system",
            "content": prompt
        },
        # User message for the assistant to respond to
        {
            "role": "user",
            "content": user_input
        }
    ],

    # Language model to generate the completion
    model="groq/compound",

    # Optional parameters
    temperature=0.0,  # Controls randomness (lower = more deterministic)
    max_completion_tokens=4096,  # Maximum number of tokens in response
#    top_p=1,  # Nucleus sampling (controls diversity)
#    stop=None,  # Stop sequence (None means no predefined stopping point)
#    stream=False  # Whether to stream partial responses
)

user_input = input()